# YouTube Transcript Parser

Скрипт для массовой загрузки автосубтитров (ASR) с YouTube, включая видео, где не работают стандартные библиотеки (`yt-dlp`, `youtube-transcript-api`).

## 🚀 Возможности

- Обход ограничений YouTube API с помощью `youtubei/v1/player`
- Поддержка нескольких API-ключей и user-agent'ов
- Стабильный `batch_fetch` с повторами, паузами и логикой ошибок
- Сохраняет текст, заголовки, дату публикации, количество просмотров и длительность
- Экспорт в `.json` для анализа в ML/LLM

## 📦 Структура

- `fetch_subs.py` — парсит субтитры одного видео
- `batch_fetch.py` — обходит список видео из `video_db.json`
- `export_combined_json.py` — собирает итоговые `.json` по каналам

## 🛠 Требования

```bash
pip install -r requirements.txt
